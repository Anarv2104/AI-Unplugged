# Local backend config for free testing
PORT=8000

# Firebase Admin SDK
# Generate from Firebase Console -> Project settings -> Service accounts -> Generate new private key
# Store the downloaded JSON inside backend/ and point to it here.
FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccount.json

# Brevo mail settings
# Rotate the API key if it has ever been shared publicly.
BREVO_API_KEY=replace-with-rotated-brevo-api-key
BREVO_SENDER_EMAIL=your-verified-sender@example.com
BREVO_SENDER_NAME=AI Unplugged

# Comma-separated bootstrap admins who should become admins on first sync/login
BOOTSTRAP_ADMIN_EMAILS=founder@example.com,ops@example.com
